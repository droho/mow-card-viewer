# CV-REL-01C RC1 — domain/repository readiness

## Frozen target

- production repository: `droho/mow-card-viewer`;
- public hostname: `cards.mowfleetbuilder.com`;
- GitHub Pages target: repository `main` branch / root;
- repository CNAME file: `cards.mowfleetbuilder.com`;
- intended DNS record: `cards` CNAME to `droho.github.io`.

## Read-only evidence gathered in CV-REL-01C

- `droho/mow-card-viewer` currently returns GitHub `404 Not Found`, so no existing production repository would be overwritten;
- the existing production Fleet Builder repository uses root custom domain `mowfleetbuilder.com`, confirming the intended domain family;
- no `cards.mowfleetbuilder.com` reference was found in the checked Man O' War GitHub repositories;
- public web search returned no indexed `cards.mowfleetbuilder.com` site.

## DNS caveat / mandatory D-stage preflight

The current execution sandbox cannot perform authoritative external DNS resolution. Therefore no claim is made that the DNS zone is currently empty.

Before the first DNS write in CV-REL-01D, perform a read-only authoritative lookup for `cards.mowfleetbuilder.com` (A/AAAA/CNAME/TXT as applicable). If any unexpected record or ownership conflict exists, STOP publication and return to central decision. Do not overwrite an existing record blindly.

If GitHub Pages requires account-level domain verification, use only the TXT challenge generated by GitHub at publication time; no verification token is invented or precommitted in this RC.
